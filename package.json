{
  "name": "cloudflare-worker-pixel-router",
  "version": "1.0.0",
  "description": "Cloudflare Worker for routing conversion pixels based on scrub percentage.",
  "main": "src/handler.ts",
  "scripts": {
    "build": "tsc",
    "deploy": "wrangler publish",
    "dev": "wrangler dev"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250414.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
